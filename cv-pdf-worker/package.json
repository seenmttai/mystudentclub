{
  "name": "cv-pdf-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker that generates ATS-friendly PDFs from HTML using Puppeteer",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.15"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
